## 2024-05-21 - Form Wrapping for Interactions
**Learning:** Wrapping input and button elements in a `<form>` tag is critical for basic usability. It enables submission via the "Enter" key without extra event handlers.
**Action:** Always wrap interactive inputs and action buttons in a `<form>` when the user intent is submission, even for single-field interfaces.
